from random import random
from Vector import *

def gen(n):
    """generating random vector with coords 0-2"""
    return [(Vector(random(), random(), random())*2).coords for a in range(n)]
    
def idealRoute():
    drone.pos
    target.pos
    
droneRoute = pos-ok sorozata  